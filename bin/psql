#!/usr/bin/env bash

case $1 in
  '--start')
  ./psql-scripts/start
    exit 0
    ;;
  '--stop')
  ./psql-scripts/stop
    exit 0
    ;;
esac

DB_NAME=${1:-"postgres"}

docker exec -it my-postgres psql -U postgres $DB_NAME
